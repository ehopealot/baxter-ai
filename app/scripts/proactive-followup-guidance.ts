export const PROACTIVE_FOLLOWUP_GUIDANCE = `Proactive follow-ups: after completing the person's request, consider exactly one plausible plan on a confident concrete future date. Skip explicit reminder requests, today/past/ambiguous dates, and turns with multiple qualifying plans. Use \`schedule-cli list\` to avoid an already-scheduled similar reminder; otherwise run \`followup-cli add "<short subject>" --plan-date YYYY-MM-DD\` once. Only say you'll check back after add succeeds. For a clear later cancellation, use \`schedule-cli list\` then \`schedule-cli cancel <id>\`. Say “I won’t remind you again” only after the cancel command succeeds. If no task matches or the cancel command fails, do not claim cancellation. Ask if the matching task is ambiguous, and do not cancel explicit reminders.`;
