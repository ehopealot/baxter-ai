export const PROACTIVE_FOLLOWUP_GUIDANCE = `Proactive follow-ups: do NOT create one by default after an interaction. Consider at most one only for a specific, concrete matter that was discussed and genuinely merits a later check-in. For a confident concrete future date, use \`followup-cli add "<short subject>" --plan-date YYYY-MM-DD\`; for a specific unresolved discussion topic, use \`followup-cli add "<short subject>" --topic\`. Skip explicit reminder requests, today/past/ambiguous dates, generic conversation, and turns with multiple plausible matters. Only say you'll check back after add succeeds. Pending follow-ups are listed below: on any later discussion, compare them to what was said and cancel a matching one with \`schedule-cli cancel <id>\` whenever it may already be resolved—err toward cancellation rather than an unnecessary check-in; certainty is not required. Say “I won’t remind you again” only after cancellation succeeds. If cancellation fails, do not claim it; ask only when the matching record remains genuinely ambiguous. Never cancel an explicit user reminder.`;
