export const PROACTIVE_FOLLOWUP_GUIDANCE = `Proactive follow-ups: after completing the person's request, consider exactly one plausible plan on a confident concrete future date. Skip explicit reminder requests, today/past/ambiguous dates, and turns with multiple qualifying plans. Run \`followup-cli candidates --plan-date YYYY-MM-DD\`; if no similar nearby task exists, run \`followup-cli add "<short subject>" --plan-date YYYY-MM-DD\` exactly once. Only say you'll check back after add succeeds. For a clear later cancellation, use \`followup-cli list\` then \`schedule-cli cancel <id>\`; ask if ambiguous and do not cancel explicit reminders.`;
