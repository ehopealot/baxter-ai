{
  "name": "mail-agent",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "dependencies": {
    "@chat-adapter/shared": "4.36.0",
    "@discordjs/voice": "^0.19.2",
    "@openrouter/agent": "^0.7.2",
    "@resend/chat-sdk-adapter": "0.2.2",
    "aws4fetch": "^1.0.20",
    "better-sqlite3": "13.0.3",
    "chat": "4.36.0",
    "cron-parser": "^4.9.0",
    "discord.js": "^14.16.3",
    "opusscript": "^0.0.8",
    "prism-media": "^1.3.5",
    "proper-lockfile": "^4.1.2",
    "resend": "6.9.2"
  },
  "optionalDependencies": {
    "@snazzah/davey-linux-arm64-gnu": "0.1.12",
    "@snazzah/davey-linux-x64-gnu": "0.1.12"
  },
  "comment_voice_deps": "Voice surface (voice-bot.ts): @discordjs/voice needs an Opus encoder -- opusscript (pure-JS, no native build; fine for one stream), used both to encode TTS playback and (via prism-media's opus.Decoder, declared directly since voice-bot.ts imports prism-media rather than relying on the @discordjs/voice transitive) to DECODE received audio for STT -- and encryption, covered by node's built-in aes-256-gcm (a Discord voice mode; verified via generateDependencyReport, so no sodium lib needed). @snazzah/davey (DAVE E2EE) ships a per-platform NATIVE binding as an optionalDependency of davey itself; the npm optional-deps bug (npm/cli#4828) can skip it, so BOTH glibc bindings are pinned here as direct optionalDependencies -- npm installs whichever matches the build arch and skips the other (EBADPLATFORM is non-fatal for optional deps), keeping cross-arch builds working like codapi/invisible; the Dockerfile's `require('@snazzah/davey')` check (in the `voice-1` stage, so only `WITH_VOICE=1` builds) still fails the build loudly if the matching binding is missing. Piper (TTS) is a binary fetched + arch-selected in the Dockerfile, not an npm dep.",
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2"
  }
}
