# Copy to .devcontainer/.env and fill in real values. .env is gitignored and
# dockerignored; only .env.example is meant to be committed.
# Loaded into the dev container automatically via `--env-file .devcontainer/.env`
# if present (by `make dev`, which runs from the repo root).
#
# EXAMPLE_API_KEY=changeme
